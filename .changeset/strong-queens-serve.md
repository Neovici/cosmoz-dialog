---
"@neovici/cosmoz-dialog": patch
---

The dialog body now scrolls its content while keeping the actions row (e.g. buttons) pinned to the bottom. Works correctly whether the body contains a single element or a content + actions pair.
