---
"@neovici/cosmoz-dialog": major
---

Remove deprecated `title` prop and attribute (breaking): use `heading` instead. Add `subtitle` and `icon` props for the title bar. The close button is now a `cosmoz-button` (tertiary, sm) with the `xCloseIcon` icon and exposes a `close` part.
